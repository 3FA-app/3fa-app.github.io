# HTTP response headers for static hosts that honour a `_headers` file
# (Cloudflare Pages, Netlify). The Content-Security-Policy itself is emitted
# per-page as a <meta> tag by Astro's CSP feature (with per-script hashes);
# these are the complementary transport/UA hardening headers.

/*
  Strict-Transport-Security: max-age=63072000; includeSubDomains; preload
  X-Content-Type-Options: nosniff
  X-Frame-Options: DENY
  Referrer-Policy: no-referrer
  Cross-Origin-Opener-Policy: same-origin
  Cross-Origin-Resource-Policy: same-origin
  Cross-Origin-Embedder-Policy: require-corp
  Permissions-Policy: accelerometer=(), camera=(), geolocation=(), gyroscope=(), magnetometer=(), microphone=(), payment=(), usb=(), interest-cohort=()
  X-Permitted-Cross-Domain-Policies: none

# Long-cache immutable build assets.
/_astro/*
  Cache-Control: public, max-age=31536000, immutable
