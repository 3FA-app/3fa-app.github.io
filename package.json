{
  "name": "threefa-website",
  "type": "module",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "test": "node --test \"tests/*.test.mjs\"",
    "test:e2e": "playwright test",
    "test:puppeteer": "node --test \"tests/puppeteer/*.test.mjs\""
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^6.4.6"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "puppeteer": "^24.0.0"
  }
}
